services:
  web:
    build: .
    image: ghcr.io/instax-dutta/cv-extractor-webapp:latest
    ports:
      - "8000:8000"
    volumes:
      - uploads:/app/uploads
    env_file:
      - .env
    restart: unless-stopped

volumes:
  uploads:
